class Solution {
    void pushZerosToEnd(int[] arr) {
        int n=arr.length;
        int nums=0;
        for(int i=0;i<n;i++){
            if(arr[i]!=0){
                arr[nums++]=arr[i];
            }
        }
        while(nums<n){
            arr[nums++]=0;
        }
    }
}